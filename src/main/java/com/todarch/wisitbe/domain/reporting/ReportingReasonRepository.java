package com.todarch.wisitbe.domain.reporting;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ReportingReasonRepository extends JpaRepository<ReportingReason, Integer> {
}
